pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Hello from Jenkins Pipeline!'
                sh 'ls -l'  # Linux (use `bat 'dir'` for Windows)
            }
        }
    }
}
